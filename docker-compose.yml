version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-app
    ports:
      - "8000:80"
    volumes:
      - .:/app
    restart: unless-stopped
